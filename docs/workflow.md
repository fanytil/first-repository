# Руководство по работе с GitHub для начинающих

## Что такое GitHub?

GitHub — это платформа для хранения кода и совместной работы над проектами. Это как "Google Drive" для программистов, где можно:
- Хранить код
- Отслеживать изменения
- Работать в команде
- Показывать свои проекты другим

---

## Шаг 1: Установка Git

Git — это программа, которая позволяет работать с GitHub на вашем компьютере.

### Для macOS:
```bash
# Установка через Homebrew (если установлен)
brew install git

# Или скачайте с официального сайта:
# https://git-scm.com/download/mac
```

### Для Windows:
Скачайте Git с официального сайта: https://git-scm.com/download/win

### Проверка установки:
```bash
git --version
```
Если видите версию (например, `git version 2.39.0`), значит Git установлен!

---

## Шаг 2: Первоначальная настройка Git

После установки нужно один раз настроить Git, указав ваше имя и email:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

**Важно:** Email должен совпадать с email, который вы используете на GitHub!

### Проверка настройки:
```bash
git config --global user.name
git config --global user.email
```

---

## Шаг 3: Создание аккаунта на GitHub

1. Перейдите на https://github.com
2. Нажмите "Sign up" (Зарегистрироваться)
3. Заполните форму регистрации
4. Подтвердите email

---

## Шаг 4: Основные команды Git

### Инициализация репозитория (создание нового проекта)

Если вы начинаете новый проект в папке:

```bash
cd /путь/к/вашей/папке
git init
```

Эта команда создаст скрытую папку `.git`, которая будет отслеживать все изменения.

### Проверка статуса проекта

```bash
git status
```

Эта команда показывает:
- Какие файлы изменены
- Какие файлы добавлены
- Какие файлы готовы к коммиту

### Добавление файлов в "staging area" (подготовка к сохранению)

```bash
# Добавить один файл
git add имя_файла.txt

# Добавить все файлы в папке
git add .

# Добавить все файлы определенного типа
git add *.js
```

**Что такое staging area?** Это как "корзина покупок" — вы выбираете файлы, которые хотите сохранить в следующем коммите.

### Создание коммита (сохранение изменений)

```bash
git commit -m "Описание того, что вы изменили"
```

**Что такое коммит?** Это "снимок" вашего проекта в определенный момент времени. Каждый коммит должен иметь понятное описание.

### Просмотр истории коммитов

```bash
git log
```

Показывает все коммиты с описаниями, авторами и датами.

---

## Шаг 5: Как писать хорошие описания коммитов

### ❌ Плохие примеры:
```
git commit -m "изменения"
git commit -m "fix"
git commit -m "обновление"
git commit -m "asdfghjkl"
```

### ✅ Хорошие примеры:
```
git commit -m "Добавлена форма регистрации пользователя"
git commit -m "Исправлена ошибка с отображением меню на мобильных устройствах"
git commit -m "Обновлена документация в файле README.md"
git commit -m "Добавлена валидация email при регистрации"
```

### Правила написания описаний:

1. **Используйте повелительное наклонение** (как будто командуете):
   - ✅ "Добавить функцию"
   - ❌ "Добавлена функция"

2. **Будьте конкретны**:
   - ✅ "Исправить баг с кнопкой отправки формы"
   - ❌ "Исправить баг"

3. **Пишите на русском или английском** (выберите один язык и придерживайтесь его)

4. **Первая строка — краткое описание** (до 50 символов идеально, до 72 максимум)

5. **Если нужно больше деталей**, используйте многострочное описание:
```bash
git commit -m "Добавить форму обратной связи

- Создан компонент ContactForm
- Добавлена валидация полей
- Интегрирован с API для отправки сообщений"
```

---

## Шаг 6: Работа с удаленным репозиторием (GitHub)

### Подключение к репозиторию на GitHub

Если репозиторий уже создан на GitHub:

```bash
# Добавить удаленный репозиторий (делается один раз)
git remote add origin https://github.com/ваш_username/название_репозитория.git

# Проверить подключенные репозитории
git remote -v
```

### Отправка изменений на GitHub

```bash
# Отправить коммиты на GitHub (в первый раз)
git push -u origin main

# В последующие разы (после первого push)
git push
```

**Что делает `push`?** Отправляет ваши локальные коммиты на сервер GitHub.

### Получение изменений с GitHub

```bash
# Скачать изменения с GitHub
git pull
```

**Что делает `pull`?** Скачивает изменения, которые были сделаны на GitHub (вами или другими участниками проекта).

---

## Шаг 7: Типичный рабочий процесс (workflow)

### Ежедневная работа над проектом:

```bash
# 1. Перейти в папку проекта
cd /путь/к/проекту

# 2. Получить последние изменения с GitHub (если работаете в команде)
git pull

# 3. Внести изменения в файлы (редактировать код)

# 4. Проверить, что изменилось
git status

# 5. Добавить измененные файлы
git add .

# 6. Создать коммит с понятным описанием
git commit -m "Описание ваших изменений"

# 7. Отправить на GitHub
git push
```

### Создание нового файла:

```bash
# 1. Создать файл (например, через редактор)
# 2. Проверить статус
git status

# 3. Добавить файл
git add новый_файл.txt

# 4. Создать коммит
git commit -m "Добавить новый файл с документацией"

# 5. Отправить на GitHub
git push
```

---

## Шаг 8: Полезные команды

### Отмена изменений

```bash
# Отменить изменения в файле (до добавления в staging)
git checkout -- имя_файла.txt

# Убрать файл из staging area (но сохранить изменения)
git reset HEAD имя_файла.txt

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1
```

### Просмотр изменений

```bash
# Посмотреть, что изменилось в файлах
git diff

# Посмотреть изменения в конкретном файле
git diff имя_файла.txt
```

### Работа с ветками (branches)

**Что такое ветка?** Ветка — это независимая линия разработки. Позволяет работать над разными функциями параллельно, не мешая друг другу.

#### Основные команды для работы с ветками

```bash
# Создать новую ветку
git branch название_ветки

# Переключиться на ветку
git checkout название_ветки

# Создать и сразу переключиться на новую ветку (современный способ)
git checkout -b название_ветки

# Или используя новую команду (Git 2.23+)
git switch -c название_ветки

# Посмотреть все ветки
git branch

# Посмотреть все ветки (включая удаленные)
git branch -a

# Удалить ветку (только если изменения уже влиты)
git branch -d название_ветки

# Удалить ветку принудительно (даже если не влита)
git branch -D название_ветки

# Переименовать текущую ветку
git branch -m новое_название

# Отправить ветку на GitHub
git push -u origin название_ветки

# Удалить ветку на GitHub
git push origin --delete название_ветки
```

#### Типичные названия веток

- **main/master** — основная ветка с рабочим кодом
- **develop** — ветка разработки, куда вливаются новые функции
- **feature/название** — ветка для новой функции (например: `feature/user-login`)
- **bugfix/название** — ветка для исправления бага (например: `bugfix/menu-display`)
- **hotfix/название** — срочное исправление в production
- **release/версия** — подготовка к релизу (например: `release/v1.0.0`)

#### Примеры работы с ветками

**Создание ветки для новой функции:**
```bash
# 1. Убедитесь, что вы на ветке develop (или main)
git checkout develop
git pull

# 2. Создайте новую ветку для функции
git checkout -b feature/add-user-profile

# 3. Работайте над функцией, делайте коммиты
git add .
git commit -m "Добавить форму профиля пользователя"
git commit -m "Добавить валидацию полей профиля"

# 4. Отправьте ветку на GitHub
git push -u origin feature/add-user-profile

# 5. После завершения работы создайте Pull Request на GitHub
# или влейте изменения обратно в develop:
git checkout develop
git merge feature/add-user-profile
git push

# 6. Удалите локальную ветку (после влития)
git branch -d feature/add-user-profile
```

**Создание ветки для исправления бага:**
```bash
# 1. Переключитесь на основную ветку
git checkout main
git pull

# 2. Создайте ветку для бага
git checkout -b bugfix/fix-login-error

# 3. Исправьте баг и закоммитьте
git add .
git commit -m "Исправить ошибку при логине с неверным паролем"

# 4. Отправьте на GitHub
git push -u origin bugfix/fix-login-error
```

**Слияние веток (merge):**
```bash
# 1. Переключитесь на ветку, в которую хотите влить изменения
git checkout main

# 2. Влейте изменения из другой ветки
git merge feature/add-user-profile

# 3. Если конфликтов нет, отправьте на GitHub
git push
```

**Решение конфликтов при слиянии:**
```bash
# Если при merge возникли конфликты:
# 1. Git покажет файлы с конфликтами
git status

# 2. Откройте файлы с конфликтами в редакторе
# Вы увидите маркеры:
# <<<<<<< HEAD
# ваш код
# =======
# код из другой ветки
# >>>>>>> feature/add-user-profile

# 3. Выберите нужный код или объедините оба варианта
# 4. Удалите маркеры конфликта
# 5. Сохраните файл
# 6. Добавьте разрешенные файлы
git add имя_файла.txt

# 7. Завершите слияние
git commit -m "Разрешить конфликты при слиянии feature/add-user-profile"
```

**Сравнение веток:**
```bash
# Посмотреть различия между ветками
git diff main..develop

# Посмотреть коммиты, которые есть в одной ветке, но нет в другой
git log main..develop
```

#### Стратегии работы с ветками

**1. GitHub Flow (простая стратегия):**
- `main` — всегда рабочий код
- Для каждой функции создается отдельная ветка
- После завершения создается Pull Request
- После проверки вливается в `main`

**2. Git Flow (для больших проектов):**
- `main` — production код
- `develop` — ветка разработки
- `feature/*` — новые функции
- `release/*` — подготовка к релизу
- `hotfix/*` — срочные исправления

**3. Простая стратегия (для маленьких проектов):**
- `main` — основной код
- Ветки создаются только для больших изменений
- Остальное делается прямо в `main`

---

## Шаг 9: Клонирование существующего репозитория

Если вы хотите скачать проект с GitHub на свой компьютер:

```bash
git clone https://github.com/username/название_репозитория.git
```

Эта команда:
1. Создаст папку с названием репозитория
2. Скачает весь код
3. Настроит подключение к GitHub

---

## Шаг 10: Решение типичных проблем

### Ошибка: "fatal: not a git repository"

**Проблема:** Вы не в папке с Git репозиторием.

**Решение:** Перейдите в папку проекта или выполните `git init`

### Ошибка: "fatal: remote origin already exists"

**Проблема:** Репозиторий уже подключен.

**Решение:** 
```bash
# Удалить старое подключение
git remote remove origin

# Добавить новое
git remote add origin https://github.com/username/repo.git
```

### Ошибка при push: "failed to push some refs"

**Проблема:** На GitHub есть изменения, которых нет у вас локально.

**Решение:**
```bash
# Сначала получить изменения
git pull

# Разрешить конфликты (если есть)
# Затем отправить
git push
```

### Забыли добавить файл в последний коммит

```bash
# Добавить файл
git add забытый_файл.txt

# Добавить к последнему коммиту
git commit --amend --no-edit
```

---

## Шаг 11: Создание репозитория на GitHub через веб-интерфейс

1. Зайдите на https://github.com
2. Нажмите кнопку "+" в правом верхнем углу → "New repository"
3. Введите название репозитория
4. Выберите "Public" или "Private"
5. **НЕ** ставьте галочки на "Initialize this repository with a README" (если у вас уже есть код)
6. Нажмите "Create repository"
7. Скопируйте URL репозитория (например: `https://github.com/username/repo.git`)
8. Выполните команды, которые GitHub покажет на странице

---

## Шаг 12: Структура хорошего проекта

Рекомендуемые файлы в проекте:

- **README.md** — описание проекта, инструкции по установке и использованию
- **.gitignore** — список файлов, которые Git должен игнорировать (пароли, временные файлы и т.д.)
- **LICENSE** — лицензия проекта (опционально)

### Создание .gitignore

Создайте файл `.gitignore` в корне проекта:

```
# Примеры для разных языков:

# Python
__pycache__/
*.pyc
.env
venv/

# Node.js
node_modules/
.env
dist/

# Общие
.DS_Store
*.log
.vscode/
.idea/
```

---

## Шпаргалка: Самые важные команды

```bash
# Настройка (один раз)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"

# Ежедневная работа
git status                    # Проверить статус
git add .                     # Добавить все файлы
git commit -m "Описание"      # Сохранить изменения
git push                      # Отправить на GitHub
git pull                      # Получить с GitHub

# Создание проекта
git init                      # Инициализировать репозиторий
git remote add origin URL     # Подключить к GitHub
git push -u origin main       # Первая отправка
```

---

## Дополнительные ресурсы

- Официальная документация Git: https://git-scm.com/doc
- Интерактивный учебник: https://learngitbranching.js.org/
- GitHub Guides: https://guides.github.com/

---

## Вопросы и ответы

**Q: Что делать, если случайно удалил файл?**
A: Используйте `git checkout -- имя_файла.txt` для восстановления из последнего коммита.

**Q: Как посмотреть, кто изменил файл?**
A: `git blame имя_файла.txt` покажет автора каждой строки.

**Q: Можно ли отменить push?**
A: Да, но осторожно! Используйте `git revert` для безопасной отмены.

**Q: Что такое merge conflict?**
A: Это когда Git не может автоматически объединить изменения. Нужно вручную выбрать, какие изменения оставить.

---

**Удачи в работе с GitHub! 🚀**

